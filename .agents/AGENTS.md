# Project Rules

## Scope Boundary
- ONLY modify files within the `frontend/` directory.
- DO NOT modify any files or directories outside `frontend/` (e.g., `backend/`, `mobile/`, root config files, etc.).
